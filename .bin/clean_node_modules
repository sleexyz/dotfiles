#!/bin/bash

read -p 'Are you sure you want to delete all ./**/node_modules?' -n 1 -r
echo    # (optional) move to a new line
if [[ $REPLY =~ ^[Yy]$ ]]
then
    find . -name 'node_modules' -exec rm -rf '{}' +
fi
